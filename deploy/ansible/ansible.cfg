[defaults]
inventory = inventory.yml
roles_path = roles
# Vault-Passwort aus Datei (gitignored, ausserhalb des Repos). Ermoeglicht
# unbeaufsichtigte Deploys ohne --ask-vault-pass.
vault_password_file = ~/.proudig-vault-pass
