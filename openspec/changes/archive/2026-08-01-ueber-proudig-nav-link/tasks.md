## 1. Navbar

- [x] 1.1 Helfer `goToUeber` in `Navbar.jsx` ergänzen (`navigate('/seite/ueber-proudig')`, Mobil-Menü schließen).
- [x] 1.2 Desktop-Eintrag „Über Proudig" von `scrollTo('ueber')` auf `goToUeber` umstellen.
- [x] 1.3 Mobil-Eintrag „Über Proudig" von `scrollTo('ueber')` auf `goToUeber` umstellen.

## 2. Footer

- [x] 2.1 Link „Über Proudig" → `/seite/ueber-proudig` in `Footer.jsx` ergänzen.

## 3. Verifikation

- [x] 3.1 Frontend-Testsuite läuft grün (`npm run test:run`).
- [x] 3.2 Manuell prüfen: Navbar (Desktop/Mobil) und Footer führen auf die veröffentlichte Seite.
